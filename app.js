// require statement in order to bring in pets objects.
let pets = require('./pets.js')
// set variables for each object and grab them by their index.
let dog = pets[0]
let cat = pets[1]
// this will rendering the objects info in the terminal.
console.log(dog, cat)



